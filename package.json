{
  "name": "thiweb-crypt-decrypt-ts",
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc && vite build",
    "prepare": "npx web-ext build --source-dir ./dist/"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^1.0.13",
    "typescript": "^4.7.4",
    "vite": "^3.0.4"
  },
  "dependencies": {
    "web-ext": "^7.1.1"
  }
}
